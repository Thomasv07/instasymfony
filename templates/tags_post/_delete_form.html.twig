<form method="post" action="{{ path('tags_post_delete', {'id': tags_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tags_post.id) }}">
    <button class="btn">Delete</button>
</form>
